# 실시간 유튜브 검색(2층) 프록시 URL. 이 값을 실제로 쓰려면
# apps/web/.env (gitignore 됨) 로 복사해 채운다.
#
# workers/youtube-search 를 Cloudflare 에 배포하면 나오는 URL:
#   VITE_YT_PROXY_URL=https://foodplay-yt-search.<서브도메인>.workers.dev
#
# 비워두면 앱은 미리 수집한 정적 풀(1층)만 쓴다 — 아무 오류 없이 동작한다.
VITE_YT_PROXY_URL=
