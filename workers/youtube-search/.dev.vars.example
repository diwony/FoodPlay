# 로컬 개발용 (`npm run dev`). 실제 값은 이 옆에 `.dev.vars` 파일로 복사해 넣는다.
# `.dev.vars` 는 gitignore 되므로 커밋되지 않는다.
# 운영 배포에서는 이 파일이 아니라 `npx wrangler secret put YT_API_KEY` 로 넣는다.
YT_API_KEY=여기에_YouTube_Data_API_키
